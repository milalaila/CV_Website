// Anda bisa menambahkan JavaScript di sini jika diperlukan
// Contoh: animasi saat scroll, interaksi tambahan, dll.
document.addEventListener('DOMContentLoaded', function() {
    console.log('CV Website loaded');
});